# T1 item 6: which preconditions are outstanding

**Generated** by `signoff/item6_preconditions.py` -- do not edit. `python3 signoff/item6_preconditions.py --check` re-derives every row below from the artifact it names and fails on any drift; it runs in `npm run check:ci`, so this document cannot go stale against the repo while the prose that used to carry these facts could.

T1 item 6 is *Statistical claims carry Monte Carlo evidence*. Its checklist text names four requirements -- *a recorded seed*, *sample count*, *a deterministic negative control*, *results combined with (not instead of) process corners* -- and `signoff/run-signoff.sh`'s guard 3 adds a fifth the cited report's own statistics must satisfy. Requirement 4 is split into two rows because its two halves live in different artifacts and are in different states.

**Verdict: 3 of 6 preconditions outstanding** (3, 4b, 5). `signoff/block-manifest.json` does not cite item 6, which is what that verdict calls for.

The campaign under grading is `sim/pll-lock-mc/records/20260924-222341-a9375a5.md` (5 draws, seeds 1..5), and the report that would be cited is `sim/pll-lock-mc/analysis/yield-evidence/klt-yield-report.json` (`status` "reported").

| # | Requirement | Verdict | Derived from | What closes it |
| --- | --- | --- | --- | --- |
| 1 | a recorded seed | met | `sim/pll-lock-mc/records/20260924-222341-a9375a5.md` -- `Trials: 5 (seeds 1..5, ...)`, cross-checked against `sim/pll-lock-mc/testbench/tb.json`'s `monte_carlo.seed_base` = 1 | already met -- one `ngspice .options seed=<N>` card per draw, recorded per trial |
| 2 | sample count | met | `sim/pll-lock-mc/records/20260924-222341-a9375a5.md` -- 5 draws; `sim/pll-lock-mc/analysis/yield-evidence/klt-yield-report.json` -- `n` = 3 measurable, `errored` = 2, `failed_unmeasurable` = 0 | already met -- both the drawn count and the measurable count are recorded (they differ, and the difference is declared rather than hidden) |
| 3 | a deterministic negative control | **unmet** | `sim/pll-lock-mc/analysis/yield-evidence/klt-yield-report.json` -- `measurements[0].negative_control` is null | a `negative_control` block in the sample-set document, populated from a seeded known-bad variant's own draws of `period_jitter_rms_pct`, plus a `klt yield` re-run whose `negative_control.verdict` is `detected` -- or a committed record arguing that `not_detected` is the honest outcome, which `run-signoff.sh`'s guard 3 names as its own escape hatch. `klt yield` is not reachable from this repo's pin (klayout-tools#2466), so the re-run is gated on that too |
| 4a | results combined with (not instead of) process corners -- the statistical axis samples process spread | met | `sim/pll-lock-mc/testbench/tb.json` -- `monte_carlo.process` = true, `monte_carlo.mismatch` = true | already met -- every draw carries die-to-die process spread (`MC_PR_SWITCH`) on top of local mismatch (`MC_MM_SWITCH`), so the population is a superset of the local-mismatch one DR-006 names |
| 4b | results combined with (not instead of) process corners -- the deterministic PVT grid has a period-jitter figure to combine them with | **unmet** | `sim/pll-lock/testbench/tb.json` -- `measure.jitter` declared (`max_frac` = 0.01); `sim/pll-lock/records/` -- **no** committed record carries a period-jitter column (looked at `20260819-122135-fe0e6df.md`, `20260904-163254-f00ce3e.md`, `20260904-165409-f3ae976.md`, `20260905-193322-0f1934d.md`) | a full-grid `sim/pll-lock` re-run against the current manifest, which is the first record that will carry the declared column. The declaration alone does not close this: a column that has never been run is not a measurement |
| 5 | the cited report's estimate is sized (`run-signoff.sh` guard 3, not item 6's checklist text) | **unmet** | `sim/pll-lock-mc/analysis/yield-evidence/klt-yield-report.json` -- `measurements[0].sample_size.verdict` = "insufficient" (`n` = 3, `required_n` = 183) | a widened campaign: 183 measurable draws against the 3 this one has. `sim/pll-lock-mc/analysis/README.md` § "The sample-size question, answered" argues why that spend is not yet the right one, and what would change that |

Read this beside `signoff/README.md` § "Why every other row is `unmet`" case 4b, which argues *why* an unmet precondition is a decline rather than a technicality, and `sim/pll-lock-mc/analysis/README.md`, which is the full read on the campaign itself.
