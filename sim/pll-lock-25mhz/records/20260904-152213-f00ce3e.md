# Record 20260904-152213-f00ce3e

- **Record ID**: 20260904-152213-f00ce3e
- **Claim**: does the closed-loop PLL (design/top/top.sch -- VCO + PFD/charge pump + loop filter + divider, issue #28), driven from a cold start (RESETB power-on, VCTRL starting at whatever the loop filter's own initial condition is) with a 25 MHz reference (spec/target-spec.md row 3's DRAFT high band edge), lock its output to N * Fref = 10 * 25 MHz = 250 MHz within this manifest's transient window -- issue #55's sibling of sim/pll-lock's own 10 MHz-reference campaign, exercising row 3's frequency-range claim at more than the single 10 MHz point sim/pll-lock/testbench/tb_pll_lock.sch drives. This is a design-input record, not a spec claim: row 3 is DRAFT/unratified. 250 MHz is deliberately the same target frequency sim/pll-lock/testbench/tb_pll_lock.sch already targets (N=25 * 10 MHz), so this record is directly comparable to that one -- only the reference frequency (and, by construction, N) changes.
- **Netlist provenance**: schematic (`sim/pll-lock-25mhz/testbench/`), frozen at `sim/pll-lock-25mhz/netlist-snapshots/20260904-152213-f00ce3e.spice`, SHA-256 `e9557776150da51c693cac74c930c22b60f97823a684a185f81168685e691fab`
- **Environment provenance**:
  - PDK: volare `sky130A`, open_pdks `c6d73a35f524070e85faff4a6a9eef49553ebc2b`
  - Model library: `/home/ubuntu/.volare/sky130A/libs.tech/combined/sky130.lib.spice`
  - Simulator: ** ngspice-46 : Circuit level simulation program; schematic capture: XSCHEM V3.4.7
  - Repo commit: `f00ce3e9ad327b2573fa1f57fb923fbe4920ecaa` (dirty)
  - Host: Linux x86_64
- **Corner matrix run**:
  - Process corners: tt
  - Temperatures (deg C): 27.0
  - Supplies (V): 1.80
  - Total points: 1
  - **Subset of the manifest's default grid**: single nominal-PVT point as an acceptable first slice for this reference-frequency band edge, matching the precedent sim/vco/records/20260819-131741-fe0e6df.md and sim/pll-lock/records/20260819-122135-fe0e6df.md set for per-point simulation cost on this host -- full-PVT-grid coverage is deferred to a follow-up issue
- **Methodology / criteria / limitations**:
  - Per-point criterion, part 1 (plumbing): ngspice exits 0, prints the harness's analysis-completion marker (`sim/harness: analysis complete`, echoed by the injected `.control` block -- ngspice does not print its own "Total analysis time" banner for a control-driven analysis), and emits no `Error:` line.
  - Per-point criterion, part 2 (measurement): the loop **locks** within the transient window. Lock criterion: mean output frequency over every sliding window of 20 output cycles stays within +/-5% of 250 MHz through the end of the transient, with at least 20 cycles of in-band data after the lock instant. Time-to-lock is the earliest instant satisfying it. A point that never satisfies it is reported as **no lock** -- its final-window mean frequency is still shown, explicitly labelled as *not* a locked output frequency, so it can never be misread as one. (Reported, but not gated on, for this manifest.)
  - Measurement method: the harness injects the analysis itself (an ngspice `.control` block built by `sim/harness/measure.py`), `linearize`s `v(CLK)` onto the transient's own 200p grid and `wrdata`-dumps it; the reducer then extracts hysteresis-guarded, linearly-interpolated threshold crossings at 0.5*VDD (the threshold tracks each point's own swept supply, not the nominal rail) and derives frequency, duty cycle and time-to-lock from them. The dumps themselves are **not** committed -- `sim/README.md`'s retention policy treats waveform data as regenerable from the frozen netlist plus the logged environment.
  - Not measured here: loop bandwidth and phase margin are open-loop quantities and need their own AC/linearized-model testbench -- see `sim/harness/measure.py`'s module docstring for why they are deliberately out of this reducer's scope.
  - DUT / limitations: this testbench's DUT is design/top/top.sch, the real four-block closed-loop PLL (not a throwaway circuit) -- straps NSEL[5:0]=001001 (N=10) and drives REF with a 25 MHz clock and RESETB with the same power-on reset pwl sim/pll-lock/testbench/tb_pll_lock.sch uses. N=10 was chosen (see sim/pll-lock-25mhz/testbench/tb_pll_lock_25mhz.sch's header comment for the arithmetic) to land the closed-loop target inside sim/vco/records/20260819-131741-fe0e6df.md's characterized VCO tuning range (~145.1 MHz to ~1.034 GHz) at exactly the same 250 MHz target sim/pll-lock/testbench/tb_pll_lock.sch already exercises with N=25 at a 10 MHz reference -- so a difference in outcome between this record and that one isolates the effect of the reference frequency (loop update rate) rather than a different target output frequency. `require_lock` is false, for the same reason sim/pll-lock's own manifest gives: a point that completes but never locks is recorded as evidence, not a harness failure. A gated pass/fail, and any resulting spec/design decision, is deferred to a future ratifying DR once this record and sim/pll-lock's own are both available to argue from.
  - Analysis: cold-start transient, closed loop (`tran 200p 3u`, injected by sim/harness/measure.py from this manifest's own measure block -- see sim/pll-lock-25mhz/testbench/tb_pll_lock_25mhz.sch's header comment for why the schematic itself carries no .tran card).
- **Result**:

  | Corner | Temp (C) | Supply (V) | Verdict | Locked | Time-to-lock | f_out (post-lock) | Duty | Detail |
  |---|---|---|---|---|---|---|---|---|
  | tt | 27 | 1.80 | FAIL | **no** | - | - | - | no oscillation: 0 rising edge(s) at v(CLK) crossed 0.9 V after 0 ps (fewer than the 4 this manifest requires) |

  - **Overall: FAIL** (0/1 points passed)
- **Links**:
  - Testbench: `sim/pll-lock-25mhz/testbench/`
  - Netlist snapshot: `sim/pll-lock-25mhz/netlist-snapshots/20260904-152213-f00ce3e.spice`
  - Raw logs: `sim/pll-lock-25mhz/corners/20260904-152213-f00ce3e/`
- **Timestamp / author**: 2026-09-04T15:23:01Z, agent-builder
- **Supersedes**: (none -- first record for this claim)
