{
  "schema": "sky130-pll.harness.tb/1",
  "claim": "Measurement-resolution floor of this harness's own period-jitter reducer, measured rather than argued (issue #178). The DUT is an ideal pulse voltage source and a load resistor -- no sky130 device at all -- so v(CLK) is a trapezoid of exactly constant period whose TRUE period jitter is zero by construction. That signal is pushed through the identical path a sim/pll-lock-mc Monte Carlo trial is reduced by: the same sim/harness/measure.py `.control` block (`linearize` onto the manifest's own `tran_step` grid, then `wrdata`), the same `parse_wrdata` -> `edge_times` (linear interpolation of the 50% crossing between the two grid samples that bracket it) -> `period_jitter` reduction, the same 200 ps grid, the same 50 us window, the same 0.5/0.15 threshold and hysteresis fractions. Whatever period jitter this campaign reports IS the pipeline's floor at that grid, because the signal it was handed has none. This is a harness negative control -- specifically a NULL control -- not a PLL design measurement, and it ratifies nothing: it exists so sim/pll-lock-mc/records/20260924-222341-a9375a5.md's measured 1.584% / 1.851% / 3.073% RMS figures against ratified row 9's 1.0% bound can be read against a measured floor instead of an unquantified one.",
  "spec_rows": [],
  "spec_rows_note": "none -- this experiment measures no property of the PLL. Its DUT is a voltage source and a resistor, and the quantity it reports is a property of sim/harness/measure.py's own reduction at a stated dump grid. It is read ALONGSIDE ratified row 9's evidence (sim/pll-lock-mc's Monte Carlo record, and in future sim/pll-lock's deterministic grid), never as evidence for or against the row itself: a floor says how much of a measured number is measurement, not whether the design meets a bound. Deliberately uncited rather than mapped to row 9, which it does not measure -- the same choice sim/pdk-smoke's plumbing self-test makes.",
  "schematic": "tb_jitter_floor.sch",
  "corner_pattern": "(\\.lib\\s+\\S*sky130\\.lib\\.spice\\s+)(\\w+)",
  "supply_pattern": null,
  "process_corners": ["tt"],
  "temps_c": [27],
  "supply_nominal": 1.8,
  "supply_tolerance": 0,
  "measure": {
    "node": "CLK",
    "tran_step": "200p",
    "tran_stop": "50u",
    "timeout_s": 1800,
    "threshold_frac": 0.5,
    "hysteresis_frac": 0.15,
    "settle_from": "0",
    "min_edges": 4,
    "require_lock": false,
    "require_oscillation": true,
    "jitter": {
      "max_frac": 0.01,
      "min_cycles": 20,
      "gate_on_bound": false
    }
  },
  "methodology_note": "THIS RECORD'S VARIANT: A3 -- pulse period 3.9170 ns (the 3.9170 ns period of the 255.3 MHz post-lock output frequency trial 5 of sim/pll-lock-mc/records/20260924-222341-a9375a5.md measured), transition time TR = TF = 20 ps (far below the 200 ps dump grid: an edge the grid cannot resolve). Read the `VCLK ... pulse(...)` card and the injected `tran` card in this record's own corners/<record-id>/tt_27c_1.80v.spice for the authoritative statement of what ran.\n\nWHY A FAMILY OF VARIANTS, AND WHAT EACH ONE IS FOR. The floor is not one number: it is a function of two ratios, and a single run would hide both. (1) The transition time divided by the grid step. When the clock's transition spans two or more grid samples, the two samples that bracket the 50% crossing both sit on the (straight) ramp and the linear interpolation in `measure.edge_times._interpolate_back` recovers the crossing exactly -- the floor collapses. When the transition is far shorter than one grid step, the bracketing pair straddles the entire edge, the interpolation is a guess, and the error is of order the grid step. (2) The period divided by the grid step. For a perfectly periodic clock the grid phase of successive edges walks by the FRACTIONAL part of that ratio, so the floor vanishes for a period that is an exact multiple of the grid step (every edge is quantized identically, so every measured period is identical) and is largest near a half-integer multiple. Both ratios are properties of the measurement, not of the design, which is exactly why they are measurable with no PLL transient at all. The variants: A1 = period 3.9952 ns (trial 2's 250.3 MHz), A2 = 3.9904 ns (trial 3's 250.6 MHz), A3 = 3.9170 ns (trial 5's 255.3 MHz), each at TR = TF = 20 ps -- one per locked draw of the Monte Carlo record, so that draw's measured figure can be restated against a floor taken at its own measured period; B = 3.9170 ns at TR = TF = 200 ps (exactly one grid step, the marginal case); C = 3.9170 ns at TR = TF = 1 ns (five grid steps, an edge the grid resolves). sim/pll-lock-mc/analysis/README.md tabulates which record each variant produced and restates the Monte Carlo record's three measured figures against them.\n\nNO `measure.lock` BLOCK, deliberately: there is no loop here and nothing to lock, so the jitter population is the whole trace from `settle_from` = 0 (every one of the ~12,700 periods in the 50 us window) rather than a post-lock tail. That is the one respect in which this control's population differs from a Monte Carlo trial's, and it cuts the right way -- a larger population, so the reported floor is an estimate with tighter sampling error than the figures it is read against, not a noisier one.\n\n`measure.jitter.gate_on_bound` IS FALSE, and `max_frac` is still row 9's ratified 1.0%: stating the bound is what makes this record's number directly comparable with the Monte Carlo record's, but gating on it would be meaningless here. A control whose job is to report how much jitter the pipeline invents for a jitter-free signal cannot sensibly FAIL for reporting a large number -- that IS the finding. (Note that `measure.py` does not gate a jitter bound on a manifest without a `lock` block in any case; `gate_on_bound: false` states the intent rather than relying on that.)\n\nWHAT THIS CONTROL DOES NOT COVER, stated because a reader of a floor number is owed it. (a) It is a null control: the signal's true jitter is zero. A signal that really does jitter presents its edges at grid phases spread by its own jitter rather than at the deterministic walk a constant period produces, so the floor a jittering signal carries is a different (and, for edges the grid cannot resolve, larger) quantity than the one measured here -- see sim/harness/measure.py's 'The dump grid puts a floor under this figure' note, and issue #185 for the calibration control that would measure it. (b) It does not establish which of the variants above the real DUT is in: nothing committed in this repo states design/vco's CLK transition time at the row-9 operating point (issue #186), so the applicable floor cannot be read off this family without that measurement. (c) Its own sampling error is not zero: a floor estimated from N periods carries roughly 1/sqrt(2N) relative error, ~0.6% relative at N = 12,700 -- immaterial at the three-decimal figures reported, but it is an estimate, not an exact constant.\n\nThe PDK/corner machinery is exercised but inert: no device in the netlist uses the sky130 .lib section the harness patches, and an ideal pulse source has no temperature or process dependence, so the single (tt, 27 C, 1.80 V) point this manifest declares is the whole grid rather than a subset of a larger one -- widening it would mint duplicate points, not coverage. `supply_pattern` is null for the same reason sim/loop-ac's is: there is no supply terminal in this testbench to patch, and adding a decorative supply source so a regex has something to match would be worse. `supply_nominal` (1.80 V) is still load-bearing -- `measure.py` takes the comparison threshold as `threshold_frac * supply_v`, so it is what puts the threshold at 0.9 V, the exact 50% point of the pulse source's 0 -> 1.8 V swing.",
  "analysis": "transient of an ideal pulse source (`tran 200p 50u`, injected by sim/harness/measure.py from this manifest's own measure block -- see sim/jitter-floor/testbench/tb_jitter_floor.sch's header comment for why the schematic itself carries no .tran card)"
}
