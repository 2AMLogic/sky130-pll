# Record 20260911-101042-7d2f839

- **Record ID**: 20260911-101042-7d2f839
- **Claim**: does design/divider/divider_intN.sch (the standalone programmable integer-N feedback divider, issue #27, retimed to a dual-modulus /2-/3 prescaler front-end by issue #114) divide a fixed 250 MHz ideal CLK by N=63 (NSEL[5:0]=111110) cleanly. N=63 is the largest *odd* modulus in the DRAFT range (spec/target-spec.md row 4, N = 4-64) -- the odd decode path exercises exactly one /3 prescaler period per output period (ODD=1) combined with the widest reload word (L=30), together the largest-depth odd-path case the decode reaches. This is a design-input measurement, not a spec/target-spec.md ratification: row 4 stays DRAFT -- per CLAUDE.md, ratification is a separate decision-record act argued on its own merits. **Spec row(s)**: 4 (DRAFT target N = 4-64, static configuration -- this record supplies evidence toward that row, not a verdict on it).
- **Netlist provenance**: schematic (`sim/divider-n63/testbench/`), frozen at `sim/divider-n63/netlist-snapshots/20260911-101042-7d2f839.spice`, SHA-256 `a87d26ffa7e86df77af0c8bf5ae462819d9f1d73772f8cee4b902d82b904ba70`
- **Environment provenance**:
  - PDK: volare `sky130A`, open_pdks `c6d73a35f524070e85faff4a6a9eef49553ebc2b`
  - Model library: `/Users/rwalters/.volare/sky130A/libs.tech/combined/sky130.lib.spice`
  - Simulator: ** ngspice-46 : Circuit level simulation program; schematic capture: XSCHEM V3.4.7
  - Repo commit: `7d2f839bf9c89864184ad9802400718c03296a76` (dirty)
  - Host: Darwin arm64
- **Corner matrix run**:
  - Process corners: tt
  - Temperatures (deg C): 27.0
  - Supplies (V): 1.80
  - Total points: 1
  - **Subset of the manifest's default grid**: issue #131: minimum-coverage subset per issue #129's named corners for the odd-decode-path near-ceiling (N=63); this invocation covers point 1 of 2 (tt/27C/1.80V nominal-only) -- see the paired ss/125C invocation for point 2 (which also covers the local supply axis at that corner/temp)
- **Methodology / criteria / limitations**:
  - Per-point criterion, part 1 (plumbing): ngspice exits 0, prints the harness's analysis-completion marker (`sim/harness: analysis complete`, echoed by the injected `.control` block -- ngspice does not print its own "Total analysis time" banner for a control-driven analysis), and emits no `Error:` line.
  - Per-point criterion, part 2 (measurement): the loop **locks** within the transient window. Lock criterion: mean output frequency over every sliding window of 5 output cycles stays within +/-2% of 3.96825 MHz through the end of the transient, with at least 5 cycles of in-band data after the lock instant. Time-to-lock is the earliest instant satisfying it. A point that never satisfies it is reported as **no lock** -- its final-window mean frequency is still shown, explicitly labelled as *not* a locked output frequency, so it can never be misread as one.
  - Measurement method: the harness injects the analysis itself (an ngspice `.control` block built by `sim/harness/measure.py`), `linearize`s `v(FBCLK)` onto the transient's own 40p grid and `wrdata`-dumps it; the reducer then extracts hysteresis-guarded, linearly-interpolated threshold crossings at 0.5*VDD (the threshold tracks each point's own swept supply, not the nominal rail) and derives frequency, duty cycle and time-to-lock from them. The dumps themselves are **not** committed -- `sim/README.md`'s retention policy treats waveform data as regenerable from the frozen netlist plus the logged environment.
  - Not measured here: loop bandwidth and phase margin are open-loop quantities and need their own AC/linearized-model testbench -- see `sim/harness/measure.py`'s module docstring for why they are deliberately out of this reducer's scope, and `sim/loop-ac/` for the campaign that does measure them.
  - DUT / limitations: this testbench's DUT is design/divider/divider_intN.sch driven open-loop (no VCO, PFD/charge pump or loop filter in the path) -- CLK is an ideal rail-to-rail pulse source (12.5 ps edges, 4 ns period = 250 MHz, 50% duty, the design's own lock target) instead of the real VCO's output, and NSEL[5:0]=111110 (N=63) is strapped statically via lab_pin ties to VDD/GND. RESETB is released by a pwl at 5-6 ns (design/divider/DESIGN.md's own issue #114 diagnostic method). `measure.lock`'s target_hz (3968253.97 Hz = f_CLK/N = 250 MHz/63) plus a 2% tolerance and a 5-output-period window/hold reproduces design/divider/DESIGN.md's own issue #114 pass criterion, applied via the harness's general-purpose sliding-window lock detector. `require_lock` is true. process_corners is the ratified rows 19/20 PVT grid (spec/target-spec.md, DR-003, #77) -- this manifest declares the same full 5-corner x 3-temp x 3-supply default grid every other sim/ manifest in this repo declares, but issue #129 only asks for the two named corners here (tt/27 degC/1.80 V and ss/125 degC/1.62 V) at minimum -- N=63's 252 ns output period makes its per-point cost comparable to sim/divider-n64's (see that manifest's own methodology_note for the measured ~0.89 s/simulated-ns rate this harness costs), so a full 45-point grid is not attempted here; issue #130 tracks widening this manifest's grid alongside sim/divider-n64's, if wanted. This record's own Corner matrix run / subset-reason field states exactly which points were run.
  - Analysis: transient, standalone divider (`tran 40p 3200n`, injected by sim/harness/measure.py from this manifest's own measure block -- see sim/divider-n63/testbench/tb_divider_n63.sch's header comment for why the schematic itself carries no .tran card). The 3.2 us window covers roughly 12.7 output periods at the target frequency (3.968 MHz, 252 ns/period) after RESETB releases at 6 ns -- more than the 11 edges (window_cycles + 1 + min_hold_cycles) the lock criterion needs..
- **Result**:

  | Corner | Temp (C) | Supply (V) | Verdict | Locked | Time-to-lock | f_out (post-lock) | Duty | Detail |
  |---|---|---|---|---|---|---|---|---|
  | tt | 27 | 1.80 | PASS | yes | 12.53 ns | 3.968 MHz | 3.2% | locked at 12.53 ns; post-lock f_out 3.968 MHz, duty 3.2% |

  - **Overall: PASS** (1/1 points passed)
- **Links**:
  - Testbench: `sim/divider-n63/testbench/`
  - Netlist snapshot: `sim/divider-n63/netlist-snapshots/20260911-101042-7d2f839.spice`
  - Raw logs: `sim/divider-n63/corners/20260911-101042-7d2f839/`
- **Timestamp / author**: 2026-09-11T10:39:26Z, agent-builder
- **Supersedes**: (none -- first record for this claim)
