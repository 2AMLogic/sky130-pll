# Record 20260925-201647-d8b1ab7

- **Record ID**: 20260925-201647-d8b1ab7
- **Claim**: ngspice integrator-error null control on design/vco (issue #202). The DUT is design/vco/vco_ring5.sch -- the ring sim/pll-lock-mc's closed loop is built on -- driven open-loop from an ideal 0.86 V DC control voltage and an ideal 1.80 V supply at tt/125 C, the Monte Carlo base point's operating point (~250 MHz). Nothing in the netlist varies with time except the ring, and a transient analysis carries no device noise, so the ring's TRUE period jitter is zero by construction: whatever period jitter this record reports is error the simulation added, not a property of the design. It is pushed through the identical path a sim/pll-lock-mc trial is reduced by -- the same sim/harness/measure.py `.control` block, the same 200 ps `tran_step` (which also caps ngspice's internal timestep), the same `linearize` -> `wrdata` -> `edge_times` -> `period_jitter` reduction, the same 0.5/0.15 threshold and hysteresis fractions. sim/jitter-floor measured the dump grid's share of such a figure with an ideal pulse source, whose edges are placed by the source's own breakpoints; a free-running ring's edges are placed by the integrator, so this is the control that measures the integrator's share, which a pulse source cannot show. It exists to test issue #202's premise -- that sim/pll-lock-mc/records/20260924-222341-a9375a5.md's 1.584% / 1.851% / 3.073% RMS figures are the circuit's -- before any design is changed. It is a harness NULL control, not a PLL design measurement, and it ratifies nothing.

THIS RECORD'S VARIANT: T -- the same DUT, grid and window as variant D with ONE change: `measure.options` = {"reltol": "1e-4"}, i.e. an `.options reltol=1e-4` card injected ahead of the analysis, tightening the timestep controller's relative-error tolerance tenfold. Everything else -- `tran_step`, dump grid, window, reducer -- is byte-for-byte variant D's, so the difference between the two records is the integrator's share and nothing else. Read the injected cards in this record's own corners/<record-id>/tt_125c_1.80v.spice for the authoritative statement of what ran.
- **Spec row(s)**: none -- none -- this experiment measures no property of the PLL. Its DUT's true period jitter is zero by construction, and the quantity it reports is a property of the simulation (ngspice's timestep control at the stated tolerances plus the harness's dump grid). It is read ALONGSIDE ratified row 9's evidence (sim/pll-lock-mc), never as evidence for or against the row itself -- the same choice sim/jitter-floor makes.
- **Netlist provenance**: schematic (`sim/integrator-floor/testbench/`), frozen at `sim/integrator-floor/netlist-snapshots/20260925-201647-d8b1ab7.spice`, SHA-256 `e86268d7701b7742ec6619c8e0c381ee9c14b18daa8f1e85ef3e051949297f58`
- **Environment provenance**:
  - PDK: volare `sky130A`, open_pdks `c6d73a35f524070e85faff4a6a9eef49553ebc2b`
  - Model library: `/Users/rwalters/.volare/sky130A/libs.tech/combined/sky130.lib.spice`
  - Simulator: ** ngspice-46 : Circuit level simulation program; schematic capture: XSCHEM V3.4.7
  - Repo commit: `d8b1ab75f8eaa206368887675c14e979a19879d3` (dirty)
  - Host: Darwin arm64
- **Corner matrix run**:
  - Process corners: tt
  - Temperatures (deg C): 125.0
  - Supplies (V): 1.80
  - Total points: 1
- **Methodology / criteria / limitations**:
  - Per-point criterion, part 1 (plumbing): ngspice exits 0, prints the harness's analysis-completion marker (`sim/harness: analysis complete`, echoed by the injected `.control` block -- ngspice does not print its own "Total analysis time" banner for a control-driven analysis), and emits no `Error:` line.
  - Per-point criterion, part 2 (measurement): the measured node `v(CLK)` oscillates -- at least 4 rising crossings of 0.5*VDD after 1 us of settling. A swept point that does not is reported as **no oscillation**, with no frequency attributed to it.
  - Measurement method: the harness injects the analysis itself (an ngspice `.control` block built by `sim/harness/measure.py`), `linearize`s `v(CLK)` onto the transient's own 200p grid and `wrdata`-dumps it; the reducer then extracts hysteresis-guarded, linearly-interpolated threshold crossings at 0.5*VDD (the threshold tracks each point's own swept supply, not the nominal rail) and derives frequency, duty cycle and time-to-lock from them. The dumps themselves are **not** committed -- `sim/README.md`'s retention policy treats waveform data as regenerable from the frozen netlist plus the logged environment.
  - Simulator accuracy: the transient runs under `.options reltol=1e-4` (manifest `measure.options`), injected ahead of the analysis, instead of ngspice's default integrator tolerances. Stated because at the defaults the timestep controller's own error shows up in a free-running ring's edge times as period jitter of the order of row 9's bound -- see `sim/harness/measure.py`'s 'The integrator's tolerance puts a second floor under this figure'.
  - Not measured here: loop bandwidth and phase margin are open-loop quantities and need their own AC/linearized-model testbench -- see `sim/harness/measure.py`'s module docstring for why they are deliberately out of this reducer's scope, and `sim/loop-ac/` for the campaign that does measure them.
  - DUT / limitations: this testbench's DUT is design/vco/vco_ring5.sch open-loop at a fixed control voltage, used as a null control: its true period is constant, so the reported period jitter is simulation error. ONE PVT point (tt/125 C/1.80 V) on purpose -- it is sim/pll-lock-mc's Monte Carlo base point, the point whose figures this control is read against; widening the grid would characterize the integrator's error elsewhere, which no claim here needs. The population starts at `settle_from` = 1 us (no lock block: there is no loop), well after the ring's start-up transient, and spans ~2,200 cycles. `gate_on_bound` is false with row 9's 1.0% still stated so the figure is directly comparable: a null control cannot sensibly FAIL for reporting a large number -- that IS the finding.
  - Analysis: transient of the open-loop ring (`tran 200p 10u`, injected by sim/harness/measure.py from this manifest's own measure block, preceded by the `.options` card when `measure.options` is declared).
- **Result**:

  | Corner | Temp (C) | Supply (V) | Verdict | f_out | Duty | Detail |
  |---|---|---|---|---|---|---|
  | tt | 125 | 1.80 | PASS | 251.1 MHz | 50.4% | f_out 251.1 MHz, duty 50.4%, period jitter 0.658% RMS over 2259 cycles |

  - **Overall: PASS** (1/1 points passed)
- **Links**:
  - Testbench: `sim/integrator-floor/testbench/`
  - Netlist snapshot: `sim/integrator-floor/netlist-snapshots/20260925-201647-d8b1ab7.spice`
  - Raw logs: `sim/integrator-floor/corners/20260925-201647-d8b1ab7/`
- **Timestamp / author**: 2026-09-25T20:24:51Z, agent-builder
- **Supersedes**: (none -- first record for this claim)
