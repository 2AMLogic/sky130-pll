{
  "schema": "sky130-pll.harness.tb/1",
  "claim": "harness self-test -- proves xschem netlisting + sim/harness PVT-point substitution (process corner, supply, temperature) + ngspice execution work end-to-end against the sky130 PDK (issue #2). This is a plumbing claim, not a PLL design claim: this testbench's DUT is a throwaway resistor divider, not the PLL (design/top/top.sch, exercised instead by the sim/pll experiment, issue #23), and the supply value below is an arbitrary smoke-test bias, not the ratified spec supply (pending #1).",
  "schematic": "tb_pdk_smoke.sch",
  "corner_pattern": "(\\.lib\\s+\\S*sky130\\.lib\\.spice\\s+)(\\w+)",
  "supply_pattern": "(V1 GND net1 )([0-9.]+)",
  "process_corners": ["tt", "ss", "ff"],
  "temps_c": [-40, 27, 125],
  "supply_nominal": 1.8,
  "supply_tolerance": 0.1,
  "methodology_note": "this testbench's DUT is a throwaway resistor divider (issue #2's harness self-test), not the PLL -- see the sim/pll experiment (issue #23) for the closed-loop PLL testbench.",
  "analysis": "DC operating point (`.op`)",
  "monte_carlo": {
    "claim": "harness self-test -- proves sim/harness's Monte Carlo trial generation + sky130 MC_MM_SWITCH/MC_PR_SWITCH statistical-sampling patching + ngspice execution work end-to-end against the sky130 PDK (issue #20). This is a plumbing claim, not a PLL statistical-spec claim: this testbench's DUT is a throwaway resistor divider, not the PLL, and spec/target-spec.md's statistical-shaped rows (period jitter row 9, reference spur row 10, supply sensitivity row 13) remain DRAFT/unratified.",
    "methodology_note": "this testbench's DUT is a throwaway resistor divider (issue #2's harness self-test), not the PLL -- see the sim/pll experiment (issue #23) for the closed-loop PLL testbench.",
    "analysis": "DC operating point (`.op`)",
    "corner": "tt",
    "temp_c": 27,
    "supply_v": 1.8,
    "mismatch": true,
    "process": true,
    "trials": 10,
    "seed_base": 1
  }
}
