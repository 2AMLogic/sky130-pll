{
  "schema": "sky130-pll.harness.tb/1",
  "claim": "Transfer of a KNOWN period-jitter figure through this harness's own period-jitter reducer, measured rather than argued (issue #185). The DUT is an ideal piecewise-linear voltage source and a load resistor -- no sky130 device at all -- but, unlike sim/jitter-floor's null control (an exactly-constant period, true period jitter zero), v(CLK)'s rising-edge schedule here is drawn from a seeded RNG at an exactly stated nonzero RMS period jitter, written edge by edge into the committed netlist by sim/jitter-calibration/testbench/gen_pwl_clock.py. That signal is pushed through the identical path a sim/pll-lock-mc Monte Carlo trial is reduced by: the same sim/harness/measure.py `.control` block (`linearize` onto the manifest's own `tran_step` grid, then `wrdata`), the same `parse_wrdata` -> `edge_times` (linear interpolation of the 50% crossing between the two grid samples that bracket it) -> `period_jitter` reduction, the same 200 ps grid, the same 0.5/0.15 threshold and hysteresis fractions. Reported-against-injected across the variant family is therefore a CALIBRATION CURVE for the reducer at this grid: a measured figure can be read back toward a true one instead of only being bounded below by a floor. The family runs at two nominal periods whose frac(period/step) sit at opposite ends of the grid-phase range (0.585 and 0.976) because the floor the reducer carries is period-specific, and the sign of the null control's error is what the second period settles (issue #197). This is a harness control -- a KNOWN-BAD control, the kind `klt yield`'s `negative_control` field and T1 item 6 ask for -- not a PLL design measurement, and it ratifies nothing.",
  "spec_rows": [],
  "spec_rows_note": "none -- this experiment measures no property of the PLL. Its DUT is a voltage source and a resistor, and the quantity it reports is a property of sim/harness/measure.py's own reduction at a stated dump grid, read against a jitter figure this testbench itself injected. It is read ALONGSIDE ratified row 9's evidence (sim/pll-lock-mc's Monte Carlo record, and in future sim/pll-lock's deterministic grid), never as evidence for or against the row itself: a calibration says how a measured number relates to a true one, not whether the design meets a bound. Deliberately uncited rather than mapped to row 9, which it does not measure -- the same choice sim/jitter-floor and sim/pdk-smoke make.",
  "schematic": "tb_jitter_calibration.sch",
  "corner_pattern": "(\\.lib\\s+\\S*sky130\\.lib\\.spice\\s+)(\\w+)",
  "supply_pattern": null,
  "process_corners": ["tt"],
  "temps_c": [27],
  "supply_nominal": 1.8,
  "supply_tolerance": 0,
  "measure": {
    "node": "CLK",
    "tran_step": "200p",
    "tran_stop": "1.25u",
    "timeout_s": 600,
    "threshold_frac": 0.5,
    "hysteresis_frac": 0.15,
    "settle_from": "0",
    "min_edges": 4,
    "require_lock": false,
    "require_oscillation": true,
    "jitter": {
      "max_frac": 0.01,
      "min_cycles": 200,
      "gate_on_bound": false
    }
  },
  "methodology_note": "THIS RECORD'S VARIANT: K20c -- see the `pwl(...)` card and the injected `tran` card in this record's own corners/<record-id>/tt_27c_1.80v.spice for the authoritative statement of what ran, and this campaign's analysis/calibration.md for the whole family in one table. The variant naming is `<period-family letter><injected RMS in tenths of a percent><edge letter>`. The period-family letters are this campaign's own: J = 3.9170 ns nominal (frac(period/step) = 0.585 at the 200 ps dump grid, issue #185), K = 3.9952 ns nominal (frac = 0.976, issue #197). The edge letters are sim/jitter-floor's: a = 20 ps (a tenth of the 200 ps dump grid, an edge the grid cannot resolve), b = 200 ps (exactly one grid step, the marginal case), c = 1 ns (five grid steps, an edge the grid resolves).\n\nWHAT THIS CONTROL IS, AND WHY IT IS NOT sim/jitter-floor AGAIN. sim/jitter-floor (issue #178) is a NULL control: its source's period is exactly constant, so its true period jitter is zero and whatever the reducer reports for it is pure measurement. That bounds the floor, but it cannot answer the question a reader of a measured jitter figure actually has -- if the signal really jitters by x, what does this pipeline report? A jitter-free source presents its edges at grid phases that walk deterministically by frac(period/step) and by nothing else; a source that genuinely jitters presents them at phases spread by its own jitter as well, which is a different distribution. So this campaign injects a KNOWN, exactly specified, nonzero figure and reports what comes back out. Reported-against-injected over the family is the calibration curve issue #185 asks for.\n\nHOW THE INJECTED FIGURE IS EXACT RATHER THAN NOMINAL. sim/jitter-calibration/testbench/gen_pwl_clock.py draws `cycles` standard normal deviates from a seeded Mersenne Twister stream (explicit Box-Muller over `Random.random()`, not `random.gauss()`, whose cached-deviate bookkeeping is a stdlib implementation detail), NORMALIZES the draw to exactly zero mean and unit population standard deviation, and sets T_k = T_nom * (1 + rms*g_k). sim/harness/measure.period_jitter reports pstdev(T_k)/mean(T_k), so that construction makes the injected figure exactly `rms` -- not an estimate of it, and with none of the sampling scatter an unnormalized draw would carry. Every rising 50% crossing is then written into the netlist as the exact midpoint of a symmetric 0 -> 1.8 V ramp, so the schedule is a readable property of the committed evidence: sim/jitter-calibration/analysis/calibration.py re-derives each record's injected figure from that record's own per-point netlist rather than trusting the generator or this note.\n\nWHY A FAMILY OF EIGHTEEN ACROSS TWO PERIODS, AND WHAT IS HELD FIXED. Two nominal periods x three injected values (0.5%, 1.0%, 2.0% RMS) x three transition times (20 ps, 200 ps, 1 ns), all at one 200 ps dump grid. Both periods are measured post-lock periods of sim/pll-lock-mc/records/20260924-222341-a9375a5.md, and both are periods sim/jitter-floor's null control has itself run at, so a reported figure here and a floor there are read at one period rather than transferred across two: family J's 3.9170 ns is trial 5's period (255.3 MHz) and sim/jitter-floor's A3/B/C variants', and family K's 3.9952 ns is trial 2's period (250.3 MHz) and sim/jitter-floor's A1 variant's. The two periods are deliberately at opposite ends of the grid-phase range -- frac(period/step) 0.585 for J, 0.976 for K -- because the walk a jitter-free clock's edges make through grid phase is set by exactly that fraction, so the floor this control measures is period-specific and issue #185's family could not say whether the null control's error keeps its sign away from the period it ran at (issue #197). The seed depends only on the injected RMS -- not on the edge and not on the period family -- so the three edge variants of a given RMS carry the IDENTICAL edge schedule and differ in nothing but the transition time (the difference between their reported figures is the grid's contribution and nothing else), and a J/K pair at one RMS and edge is built from the IDENTICAL normalized draw scaled to the two periods (the difference between their reported figures is the period's contribution and nothing else).\n\nWHAT THE K FAMILY'S PERIOD MATCHES DOWNSTREAM, stated so a later restatement can cite it rather than re-derive it. 3.9952 ns is trial 2's own measured post-lock period in sim/pll-lock-mc/records/20260924-222341-a9375a5.md, and sim/pll-lock-mc/analysis/jitter-floor/restatement.md (issue #193) names a family at exactly this period -- and/or at trial 3's 3.9904 ns -- as \"the run that would change this answer\" for the residual ambiguity it declines to resolve. This campaign supplies the run; it states no verdict on trial 2, on that restatement's Decline, or on ratified row 9, all of which remain that analysis's own to make. Trial 3's 3.9904 ns (frac = 0.952) is NOT run here: one family at one near-integer period is what the sign question needs, and 3.9952 ns is the nearer of the two to an integer multiple of the grid step.\n\nPOPULATION SIZING, and the subset question it is not. 300 cycles in a 1.25 us window, not the ~12,700 a 50 us window at this period would hold. (The window was 1.2 us for issue #185's nine family-J records, whose committed netlists say so; 300 cycles at family K's longer period run 25 ns past it, and a window that truncated the schedule would hand the reducer a smaller population than the committed schedule holds. gen_pwl_clock.py asserts every variant's last PWL point against this manifest's window rather than leaving that to inspection. The family-J records already committed are evidence of what ran then and are untouched, per sim/README.md's append-only rule.) This is not a declared subset of a larger grid -- the manifest's own grid is one PVT point and the whole of it runs -- it is the population issue #185's own sizing note calls for: a few hundred periods is plenty for an RMS estimate, and it keeps the committed edge schedule (which IS the testbench here, ~1,200 PWL points) small. The cost is sampling error on the REPORTED figure of roughly 1/sqrt(2N) = 4.1% relative; the INJECTED figure carries none, being exact by construction. The reported figures are quoted to three decimals of a percent by sim/harness/report.py, and should be read with that 4.1% relative uncertainty rather than at face precision.\n\nNO `measure.lock` BLOCK, deliberately: there is no loop here and nothing to lock, so the jitter population is the whole trace from `settle_from` = 0 (all 300 periods) rather than a post-lock tail -- the same choice, for the same reason, sim/jitter-floor makes. `measure.jitter.min_cycles` is 200 rather than the default 20 so a truncated trace cannot quietly yield a figure from a population this campaign did not intend.\n\n`measure.jitter.gate_on_bound` IS FALSE, and `max_frac` is still row 9's ratified 1.0%: stating the bound keeps this record's number directly comparable with sim/pll-lock-mc's and sim/jitter-floor's, but gating on it would be meaningless here. A control that deliberately injects 2.0% RMS cannot sensibly FAIL for reporting a large number -- that IS the finding. (`measure.py` does not gate a jitter bound on a manifest without a `lock` block in any case; `gate_on_bound: false` states the intent rather than relying on that.)\n\nWHAT THIS CONTROL DOES NOT COVER, stated because a reader of a calibration curve is owed it. (a) Its injected jitter is INDEPENDENT AND GAUSSIAN period to period -- white period jitter, no correlation, no wander, no deterministic/spur component. A real loop's jitter is not white, and a correlated source presents a different grid-phase distribution again; this campaign calibrates the reducer against the uncorrelated case only. (b) It is taken at TWO nominal periods (3.9170 ns, frac 0.585; 3.9952 ns, frac 0.976) and ONE dump grid (200 ps). The grid-phase walk depends on frac(period/step), so the numbers here transfer to a THIRD period only through the arithmetic, not by assumption -- two periods measure the sign of the error at two values of frac, not the whole curve between them (trial 3's 3.9904 ns, frac 0.952, is one such unvisited period). (c) Its reported figures carry the ~4.1% relative sampling error of a 300-cycle population, stated above. (d) It does not establish which variant the real DUT is in: nothing committed in this repo states design/vco's CLK transition time at the row-9 operating point (issue #186), so the applicable row of this table cannot be read off without that measurement -- the same gap sim/jitter-floor's records name.\n\nThe PDK/corner machinery is exercised but inert: no device in the netlist uses the sky130 .lib section the harness patches, and an ideal PWL source has no temperature or process dependence, so the single (tt, 27 C, 1.80 V) point this manifest declares is the whole grid rather than a subset of a larger one -- widening it would mint duplicate points, not coverage. `supply_pattern` is null for the same reason sim/jitter-floor's and sim/loop-ac's are: there is no supply terminal in this testbench to patch, and adding a decorative supply source so a regex has something to match would be worse. `supply_nominal` (1.80 V) is still load-bearing -- measure.py takes the comparison threshold as `threshold_frac * supply_v`, so it is what puts the threshold at 0.9 V, the exact 50% point of the PWL source's 0 -> 1.8 V swing and therefore exactly the instants the committed schedule names.",
  "analysis": "transient of an ideal piecewise-linear source (`tran 200p 1.25u`, injected by sim/harness/measure.py from this manifest's own measure block -- see sim/jitter-calibration/testbench/tb_jitter_calibration.sch's header comment for why the schematic itself carries no .tran card)"
}
