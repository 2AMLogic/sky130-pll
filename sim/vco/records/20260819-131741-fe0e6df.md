# Record 20260819-131741-fe0e6df

- **Record ID**: 20260819-131741-fe0e6df
- **Claim**: VCO frequency-vs-VCTRL characterization for design/vco/vco_ring5.sch (the standalone current-starved 5-stage ring VCO, issue #24), measured at six VCTRL points from 0.8 V to 1.6 V, matching design/vco/DESIGN.md's own informal sanity-check table (issue #52). This is a measured design-input record, not a spec claim: it replaces that informal, uncommitted, single-corner sanity check with real committed sim/ evidence, so a future decision record has something citable to argue spec/target-spec.md row 2 (output band) and row 5 (Kvco) from. It ratifies nothing -- per CLAUDE.md, ratification is a separate decision-record act argued on its own merits. The PVT grid this manifest declares is the same one sim/pll uses (tt/ss/ff x -40/27/125 degC x 1.62/1.80/1.98 V, taken from spec/target-spec.md row 1's supply range; that row's status at the time this manifest was written is stated in the record's own methodology note) -- a given record's own **Corner matrix run** field states whether it covers that full grid or a declared subset, per sim/README.md's subset-justification rule.
- **Netlist provenance**: schematic (`sim/vco/testbench/`), frozen at `sim/vco/netlist-snapshots/20260819-131741-fe0e6df.spice`, SHA-256 `9620ece6c9c0364f167374735dc9bece24d02a4d71c540f5266dd328db143064`
- **Environment provenance**:
  - PDK: volare `sky130A`, open_pdks `c6d73a35f524070e85faff4a6a9eef49553ebc2b`
  - Model library: `/home/ubuntu/.volare/sky130A/libs.tech/combined/sky130.lib.spice`
  - Simulator: ** ngspice-46 : Circuit level simulation program; schematic capture: XSCHEM V3.4.7
  - Repo commit: `fe0e6df253c4ac6a7dc6c42fa7d935517eb553ed` (dirty)
  - Host: Linux x86_64
- **Corner matrix run**:
  - Process corners: tt
  - Temperatures (deg C): 27.0
  - Supplies (V): 1.80
  - Total points: 1
  - **Subset of the manifest's default grid**: single representative corner (tt/27C/1.80V nominal) -- each VCTRL sweep point costs roughly 5 minutes of wall-clock simulation on this host (an ngspice/sky130-model convergence characteristic of this environment, not specific to any one corner), so the full 27-point tt/ss/ff x -40/27/125C x 1.62/1.80/1.98V grid this manifest declares is deferred to a follow-up issue with a longer compute budget. This subset still exercises the full measurement pipeline (frequency/duty extraction across a real swept axis) end to end.
- **Methodology / criteria / limitations**:
  - Per-point criterion, part 1 (plumbing): ngspice exits 0, prints the harness's analysis-completion marker (`sim/harness: analysis complete`, echoed by the injected `.control` block -- ngspice does not print its own "Total analysis time" banner for a control-driven analysis), and emits no `Error:` line.
  - Per-point criterion, part 2 (measurement): the measured node `v(clk)` oscillates -- at least 4 rising crossings of 0.5*VDD after 60 ns of settling. A swept point that does not is reported as **no oscillation**, with no frequency attributed to it. A characterization sweep deliberately runs past the edges of the tuning range, so an individual dead swept value is recorded as data rather than gating the point; this manifest instead requires at least 3 of the swept values to oscillate.
  - Measurement method: the harness injects the analysis itself (an ngspice `.control` block built by `sim/harness/measure.py`), `linearize`s `v(clk)` onto the transient's own 50p grid and `wrdata`-dumps it; the reducer then extracts hysteresis-guarded, linearly-interpolated threshold crossings at 0.5*VDD (the threshold tracks each point's own swept supply, not the nominal rail) and derives frequency, duty cycle and time-to-lock from them. The dumps themselves are **not** committed -- `sim/README.md`'s retention policy treats waveform data as regenerable from the frozen netlist plus the logged environment.
  - Not measured here: loop bandwidth and phase margin are open-loop quantities and need their own AC/linearized-model testbench -- see `sim/harness/measure.py`'s module docstring for why they are deliberately out of this reducer's scope.
  - DUT / limitations: this testbench's DUT is design/vco/vco_ring5.sch driven open-loop (no PFD, charge pump, loop filter or divider in the path) -- it characterizes the VCO block alone, not the closed loop, which is sim/pll's job. The six VCTRL values match design/vco/DESIGN.md's own informal sanity-check table, so this record is directly comparable to it; a swept value where the ring does not free-run is recorded as data (the edge of the range) rather than as a failure, and the per-point verdict requires at least three of the six to oscillate. Each swept point is a fresh 200 ns transient started from RING0 forced low with `uic`, because a noiseless simulator will otherwise sit on the ring's unstable all-stages-at-midrail DC equilibrium indefinitely; the first 60 ns are discarded as startup. The reported slope is an endpoint-to-endpoint mean across the whole swept range, not a local small-signal Kvco, and the curve is strongly non-linear -- read the per-swept-value table for the shape.
  - Analysis: transient per swept VCTRL value (`tran 50p 200n uic`, six values, one ngspice invocation per PVT point).
- **Result**:

  | Corner | Temp (C) | Supply (V) | Verdict | Detail |
  |---|---|---|---|---|
  | tt | 27 | 1.80 | PASS | 6/6 swept points oscillated; VCTRL=0.800V 145.1 MHz, VCTRL=0.900V 318.1 MHz, VCTRL=1.000V 510.9 MHz, VCTRL=1.200V 804.2 MHz, VCTRL=1.400V 949.8 MHz, VCTRL=1.600V 1.034 GHz |

  - **Overall: PASS** (1/1 points passed)

- **Swept-axis measurements** (VCTRL vs. output frequency, per PVT point):

  | Corner | Temp (C) | Supply (V) | VCTRL (V) | f_out | Duty | Note |
  |---|---|---|---|---|---|---|
  | tt | 27 | 1.80 | 0.800 | 145.1 MHz | 50.6% | - |
  | tt | 27 | 1.80 | 0.900 | 318.1 MHz | 50.5% | - |
  | tt | 27 | 1.80 | 1.000 | 510.9 MHz | 50.1% | - |
  | tt | 27 | 1.80 | 1.200 | 804.2 MHz | 48.0% | - |
  | tt | 27 | 1.80 | 1.400 | 949.8 MHz | 46.5% | - |
  | tt | 27 | 1.80 | 1.600 | 1.034 GHz | 46.0% | - |

- **Per-point tuning summary** (endpoint-to-endpoint slope over the swept VCTRL range):

  | Corner | Temp (C) | Supply (V) | Oscillating VCTRL range (V) | f_min | f_max | Mean slope |
  |---|---|---|---|---|---|---|
  | tt | 27 | 1.80 | 0.800-1.600 | 145.1 MHz | 1.034 GHz | 1111 MHz/V |

  The slope column is an **endpoint-to-endpoint mean over the whole swept range**, not a local small-signal gain, and the underlying curve is strongly non-linear -- read the per-swept-value table above for the shape. It is reported as characterization data; nothing here ratifies a spec row.
- **Links**:
  - Testbench: `sim/vco/testbench/`
  - Netlist snapshot: `sim/vco/netlist-snapshots/20260819-131741-fe0e6df.spice`
  - Raw logs: `sim/vco/corners/20260819-131741-fe0e6df/`
- **Timestamp / author**: 2026-08-19T13:34:17Z, agent-builder
- **Supersedes**: (none -- first record for this claim)
